# =============================================================================
# writ — Rust workspace + agentic / IDE tooling
# =============================================================================

# -----------------------------------------------------------------------------
# Rust (github/gitignore Rust template)
# -----------------------------------------------------------------------------
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
# JetBrains specific template is maintained in a separate JetBrains.gitignore
# that can be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
# and can be added to the global gitignore or merged into this file.
# .idea/

# -----------------------------------------------------------------------------
# Python (PyPI / GitHub Python template, trimmed for hybrid repo)
# -----------------------------------------------------------------------------
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask / Scrapy
instance/
.webassets-cache
.scrapy

# Sphinx / docs
docs/_build/

# PyBuilder (note: Rust also uses target/; both covered above)
.pybuilder/

# Jupyter / IPython
.ipynb_checkpoints
profile_default/
ipython_config.py

# Environments
.env
.env.*
!.env.example
.envrc
.venv
# Leftover local Qodana venv from the retired paid gate. Not a required check.
.qodana-venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Type checkers / linters
.mypy_cache/
.dmypy.json
dmypy.json
.pyre/
.pytype/
.ruff_cache/
cython_debug/

# Package managers (local state; lockfiles usually committed)
.pdm-python
.pdm-build/
.pixi/
__pypackages__/
.pypirc

# Local secrets / app configs
.streamlit/secrets.toml
.abstra/
tempCodeRunnerFile.py
marimo/_static/
marimo/_lsp/
__marimo__/

# -----------------------------------------------------------------------------
# OS / generic
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
*~
*.swp
*.swo
*.bak
*.tmp
*.temp
.cache/

# -----------------------------------------------------------------------------
# IDE & editor tooling
# -----------------------------------------------------------------------------

# Visual Studio Code / VSCodium
.vscode/
*.code-workspace
.history/

# JetBrains (RustRover, CLion, PyCharm, etc.)
.idea/
*.iml
*.ipr
*.iws
out/

# Vim / Neovim
.vim/
Session.vim
.netrwhist

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.desktop.lock
*.elc
auto-save-list
tramp
.\#*

# Sublime Text
*.sublime-project
*.sublime-workspace

# -----------------------------------------------------------------------------
# Agentic coding tools
# -----------------------------------------------------------------------------

# OpenAI Codex CLI / IDE
.codex/
**/.codex/
codex-sessions/
.codex-cache/
*.codex

# Cursor
.cursor/
**/.cursor/
.cursorignore
.cursorindexingignore
# local Cursor rules/state sometimes appear under project
.cursorrules.local

# Grok / xAI agent tooling
.grok/
**/.grok/
.grok-build/
.grok-sessions/
# session artifacts if ever written under repo
**/.grok/sessions/

# MimoCode
.mimocode/
**/.mimocode/
.mimo/
**/.mimo/
mimocode/
.mimocode-cache/

# Kilo Code / Kilo AI
.kilo/
**/.kilo/
.kilocode/
**/.kilocode/
kilo-code/
.kilo-cache/
.kilocode-cache/

# Kiro (AWS Kiro / agent IDE)
.kiro/
**/.kiro/
.kiro-agent/
**/.kiro-agent/
.kiro-sessions/
.kiro-cache/

# -----------------------------------------------------------------------------
# Local agent runtime (writ project state)
# -----------------------------------------------------------------------------
.writ-state/
.writ/
**/writ-state/
.local/share/writ/
# Legacy names from the pre-rename `wh` / worktrees-hives runtime so existing
# local dirs stay untracked after upgrade.
.worktrees-hives/
.wh/
**/wh-state/
wh-state/
.local/share/worktrees-hives/
# ad-hoc worktrees if created under the repo
.worktrees/
worktrees/
!docs/**/worktrees/

# -----------------------------------------------------------------------------
# Secrets & credentials (never commit)
# -----------------------------------------------------------------------------
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
credentials.json
secrets.json
.secrets/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# -----------------------------------------------------------------------------
# Beads local DB/export (bd); keep on disk, do not commit
# -----------------------------------------------------------------------------
.beads/

# Local/personal skill drafts (not product source)
SKILL-grok-linear.md

# OpenCode agent skills / local tooling (not product)
.opencode/
# Leftover local Qodana output dir from the retired paid gate. Not a required check.
qodana/

# Local agent / IDE MCP scratch (never commit)
.chroma_recall_status.json
.chroma_recall_tmp.py
.mcp.json

.ai/

# Qlty CLI runtime output. `qlty check` creates these inside .qlty/ as SYMLINKS
# into ~/.qlty/cache, not as directories -- so the patterns must NOT have a
# trailing slash. A trailing slash matches directories only, which is why four
# machine-specific symlinks got committed before being caught in review.
.qlty/logs
.qlty/out
.qlty/results
.qlty/plugin_cachedir
.qlty/tmp
